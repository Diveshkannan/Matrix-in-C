# Matrix-in-C
This C program initializes a 3×3 integer matrix and calculates the sums of its primary (top-left to bottom-right) and secondary (top-right to bottom-left) diagonals using loops. It then prints both results, demonstrating 2D array handling and diagonal traversal in C.
